<script lang="ts">
  import type { HTMLAttributes } from 'svelte/elements'; import { Check } from '@lucide/svelte'; import { cn } from '../../internal/cn.js'; import Button from '../foundation/Button.svelte';
  type Props=Omit<HTMLAttributes<HTMLElement>, 'children'>&{name:string;description?:string;price:string;period?:string;features:string[];featured?:boolean;badge?:string;actionLabel?:string;onaction?:()=>void}; let {name,description,price,period='/ Monat',features,featured=false,badge,actionLabel='Auswählen',onaction,class:className,...rest}:Props=$props();
</script>
<article {...rest} class={cn('rg-pricing',className)} data-featured={featured}><div>{#if badge}<span class="rg-badge" data-tone="primary">{badge}</span>{/if}<h3 class="rg-card-title" style="margin-top:.65rem">{name}</h3>{#if description}<p class="rg-card-description">{description}</p>{/if}</div><div class="rg-price"><span class="rg-price-value">{price}</span><span class="rg-price-period">{period}</span></div><ul class="rg-feature-list">{#each features as feature}<li style="display:flex;gap:.5rem"><Check size={16} color="var(--rg-success)"/><span>{feature}</span></li>{/each}</ul><Button full variant={featured?'solid':'outline'} onclick={onaction}>{actionLabel}</Button></article>

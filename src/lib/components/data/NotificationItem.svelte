<script lang="ts">
  import type { Snippet } from 'svelte'; import type { HTMLAttributes } from 'svelte/elements'; import { Bell } from '@lucide/svelte'; import { cn } from '../../internal/cn.js';
  type Props=HTMLAttributes<HTMLElement>&{title:string;description?:string;time?:string;unread?:boolean;icon?:Snippet;actions?:Snippet}; let {title,description,time,unread=false,icon,actions,class:className,...rest}:Props=$props();
</script>
<article {...rest} class={cn('rg-notification',className)} data-unread={unread}><span class="rg-state-icon" style="width:2.1rem;height:2.1rem">{#if icon}{@render icon()}{:else}<Bell size={16}/>{/if}</span><div><div style="display:flex;align-items:center;gap:.45rem"><strong style="font-size:var(--rg-text-sm)">{title}</strong>{#if unread}<span style="width:.42rem;height:.42rem;border-radius:50%;background:var(--rg-primary)" aria-hidden="true"></span><span class="rg-sr-only">Ungelesen</span>{/if}</div>{#if description}<div style="color:var(--rg-fg-muted);font-size:var(--rg-text-sm);margin-top:.15rem">{description}</div>{/if}{#if time}<time style="color:var(--rg-fg-subtle);font-size:var(--rg-text-xs)">{time}</time>{/if}</div>{#if actions}<div>{@render actions()}</div>{/if}</article>
